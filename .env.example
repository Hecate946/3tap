# Create a Resend API key after verifying 3tap.cc in Resend.
RESEND_API_KEY=re_xxxxxxxxx

# Optional overrides. Production defaults are already in wrangler.jsonc.
# RESET_EMAIL_FROM=3tap <reset@3tap.cc>
# APP_ORIGIN=http://localhost:5173
