# 3tap

a tiny three-state habit tracker I made because I wanted something simple enough to actually use myself and keep myself accountable.

```bash
npm run dev
```

`npm run site:zip` makes a clean source zip.
